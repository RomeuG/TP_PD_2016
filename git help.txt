inicio:
git clone <link https>

git branch
git branch joao
git checkout joao

git pull origin joao		// Recebe alterações no repositorio / branch

git add <filename>
git commit -m "first"
git push origin <branch>